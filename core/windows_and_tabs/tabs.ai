"""
 This file contains commands to control tabs in a browser using Talon voice recognition software. The commands include opening a new tab, moving to the previous or next tab, closing the current tab, duplicating the current tab and jumping to a specific tab using a number or to the final tab. The code also includes a wrapper function for closing tabs that allows for app-specific delays.
"""tag: user.tabs
tab (open | new): app.tab_open()
tab (last | previous): app.tab_previous()
tab next: app.tab_next()
tab close: user.tab_close_wrapper()
tab (reopen | restore): app.tab_reopen()
go tab <number>: user.tab_jump(number)
go tab final: user.tab_final()
tab duplicate: user.tab_duplicate()
