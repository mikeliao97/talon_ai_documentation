"""
 This is a Talon voice file that contains a module to handle various file extensions. The module lists different file extensions with names and assigns them to a context. The commands can be activated through voice commands using the assigned names to open files with the respective extensions.
"""{user.file_extension}: "{file_extension}"
